# config.yaml
# NEVER commit this file to version control or share it
# This is for REAL Binance mainnet (live trading)

binance:
  api_key:     "paste_your_real_futures_api_key_here"
  api_secret:  "paste_your_real_futures_api_secret_here"

trading:
  symbol:             "BTCUSDT"           # change to SOLUSDT, ETHUSDT, etc. if desired
  leverage:           5                   # start low (1–20 recommended for testing)
  margin_type:        "ISOLATED"          # or "CROSSED"
  default_quantity:   0.001               # very small – adjust according to symbol precision
